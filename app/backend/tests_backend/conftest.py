import os
import sys

# Sara do not delete this file again please!

# makes sure Python can import modules from the backend directory. Running tests from the tests_backend directory requires this.
sys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), "../")))